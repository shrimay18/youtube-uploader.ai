# --- Ingest ---
gdown>=5.1.0                 # Google Drive download via public link

# --- Transcribe ---
faster-whisper>=1.0.3       # local, free ASR with word timestamps (CTranslate2)
# NVIDIA GPU acceleration (~45x realtime vs ~6x on CPU). Installed automatically
# used when present; harmless if you have no GPU (code falls back to CPU):
nvidia-cublas-cu12          # GPU: cuBLAS runtime
nvidia-cudnn-cu12           # GPU: cuDNN runtime

# --- SEO research (free, no keys) ---
requests>=2.31.0            # YouTube autocomplete endpoint
pytrends>=4.9.2             # Google Trends (unofficial)

# --- LLM providers (engine-agnostic) ---
google-genai>=1.0.0         # Gemini free tier (default engine)
# ollama>=0.3.0             # uncomment if using local Ollama
# anthropic>=0.39.0         # uncomment if using Claude

# --- Thumbnails (Shorts) ---
opencv-python>=4.9.0,<5     # best-frame scoring (5.0 wheel drops CascadeClassifier)
Pillow>=10.2.0              # text-hook overlay
numpy>=1.26.0

# --- YouTube upload/schedule ---
google-api-python-client>=2.120.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# --- Misc ---
PyYAML>=6.0.1               # config + draft.yaml
Jinja2>=3.1.3              # HTML preview rendering
Flask>=3.0                 # editable local review UI (youtube_manager review)
python-dateutil>=2.9.0     # RFC3339 / IST timestamp handling
